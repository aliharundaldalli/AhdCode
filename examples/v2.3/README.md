# v2.3.0 release examples

These examples are intentionally small and exercise the v2.3.0 release scope:

- [Named Function `uses`](uses/README.md): explicit `#` lexical and `@` module
  captures shared with lambda semantics.
- [Plot and Surface math labels](math_plot/README.md): whole-string `$...$`
  labels through the existing offline math-text path.
