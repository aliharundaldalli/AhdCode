# Named Function `uses` (v2.3.0)

This small example shows `@activeLang` for a module binding, `#minimum` for an
enclosing local, and a nested named Function. The final lambda uses the same
capture representation; named Functions do not introduce a second closure
model.

Run with:

```bash
ahdcode run main.ahd
```
